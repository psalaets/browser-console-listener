{
  "name": "browser-console-listener",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "prebuild": "rm -rf build; mkdir build",
    "build": "browserify index.js -s browser-console-listener > build/bcl.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify": "10.2.4",
    "mocha": "2.2.5"
  }
}
